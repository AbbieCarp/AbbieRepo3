// Re-export the TaskService so consumers can import from the conventional
// camelCase path (src/services/taskService.js) as specified in the schema.
export { TaskService } from './task-service.js';
